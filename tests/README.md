# Tests

Everything must be tested

## Unit Tests (Jest)

All lines and branches must be tested.

## Component Tests (Cypress)

| Use E2E tests for components that use `getServerSideProps` or `getStaticProps` [docs](https://docs.cypress.io/guides/component-testing/react/overview#Nextjs-Caveats).

All *applicable* components must be tested.

## E2E Tests (Cypress)

All *applicable* workflows must be tested.
